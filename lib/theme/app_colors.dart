import 'package:flutter/material.dart';

class AppColors {
  // Fundo geral
  static const Color backgroundLight = Color(0xFFF8F8F0);
  static const Color backgroundDark = Color.fromARGB(255, 7, 7, 10);

  // Primárias 
  static const Color primary = Color(0xFFB1CCC4);
  static const Color secondary = Color(0xFF4B3F72);

  // Texto
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.white;

  // Sucesso - erro
  static const Color success = Color(0xFF21E6C1);
  static const Color error = Color(0xFFFF4C4C);

  // Detalhes
  static const Color details = Color.fromARGB(255, 17, 70, 54);
}
